import styled from 'styled-components'
import Card from '../Card'

export const LandingContainer = styled(Card)`
  text-align: center;
  color: inherit;
  width: 50%;
  margin: 0 auto;
  background-color: var(--color-primary);

  margin-top: -9rem;

  h2 {
    font-size: 2.5rem;
  }

  p {
    font-size: 1.6rem;
  }
`
